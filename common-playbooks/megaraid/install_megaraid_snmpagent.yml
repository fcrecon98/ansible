- hosts: '{{ hosts }}'
  sudo: no
  vars_files: 
     - ../../common-vars/common.yml
     - ../../platform-vars/{{ platform }}-{{ env }}.yml
     - ../../system-vars/{{ system }}-{{ env }}.yml

  roles:
     - ../../common-roles/snmpd/01_install_snmpd
     - ../../common-roles/megaraid/10_snmp-agent
